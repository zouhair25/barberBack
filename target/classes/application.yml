spring:
  application:
    name: barbershop-api

  datasource:
    url: jdbc:mysql://127.0.0.1:3307/barbershop?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: ${DB_USER:nabadylinux}
    password: ${DB_PASS:nabadylinux}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true


  flyway:
    enabled: false
    baseline-on-migrate: false

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USER:}
    password: ${MAIL_PASS:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  cache:
    type: simple

server:
  port: 7080
  servlet:
    context-path: /api/v1

app:
  jwt:
    secret: ${JWT_SECRET:YourSuperSecretKeyForBarbershopAppThatIsLongEnough256Bits!}
    access-token-expiry-ms: 900000
    refresh-token-expiry-ms: 604800000

  storage:
    base-path: ${STORAGE_PATH:./uploads}

  reminders:
    hours-before: 2
    check-interval-ms: 900000

  twilio:
    account-sid: ${TWILIO_SID:}
    auth-token: ${TWILIO_TOKEN:}
    from-number: ${TWILIO_FROM:}
    whatsapp-from: ${TWILIO_WA_FROM:}

  cors:
    allowed-origins: ${CORS_ORIGINS:http://localhost:7000}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
